# CH-0.4 frontend toolchain pinning evidence

## Identity

- Task: `CH-0.4`
- Started: `2026-08-13T22:40:29Z`
- Completed: `2026-08-13T23:04:14Z`
- Baseline source SHA: `87be188c`
- Implementation SHAs: `14dc5aaa`, `e954b0c7`
- Candidate source SHA: `e954b0c7cbb5337c273f116691840643f667b630`
- GitHub Actions run: `31752234413`

The first implementation commit pinned pnpm and the package-manager policy. The
follow-up commit corrected the remaining floating Node input, so the final
candidate uses the same exact Node release locally, in CI, and in Docker.

## Red baseline

- Local Node was `v22.20.0` and local pnpm was `11.5.2`.
- CI requested floating pnpm major `10` and a floating Node 22 range.
- Docker used a floating Node 22 Alpine tag and Corepack-selected pnpm.
- `.npmrc` contained `minimum-release-age=10080`, but pnpm 11.5.2 reported that
  key as `undefined`; the policy was therefore not active.
- Both `package-lock.json` and `pnpm-lock.yaml` were present.
- Before this packet, frontend lint reported 18 errors and 4 warnings, the
  strict leaf TypeScript check reported three `TS6133` diagnostics, and the
  frontend test suite had a known order-sensitive repository-highlight test.
  Those existing failures belong to CH-0.5 and CH-0.6.

## Implemented contract

- `frontend/.node-version` pins Node `22.20.0`.
- `frontend/package.json` pins `packageManager` to `pnpm@11.5.2` and constrains
  Node to `>=22.20.0 <22.21`.
- GitHub Actions consumes `.node-version`, installs pnpm `11.5.2`, and reports
  both versions before installing.
- the frontend Docker dependency stage uses `node:22.20.0-alpine`, installs
  pnpm `11.5.2`, and reports both versions before installing.
- `frontend/pnpm-workspace.yaml` now owns the effective
  `minimumReleaseAge: 10080` supply-chain policy.
- the ineffective `.npmrc` spelling was removed.
- `frontend/package-lock.json` was removed; pnpm is the sole package manager.
- no application dependency was added, removed, or upgraded.

## Verification

### Lock and local install

- `node --version` returned `v22.20.0`.
- `pnpm.cmd --version` returned `11.5.2`.
- `pnpm.cmd config get minimumReleaseAge` returned `10080`.
- `pnpm.cmd install --frozen-lockfile` passed and checked all 776 lock entries
  against the seven-day minimum release age.
- `frontend/pnpm-lock.yaml` remained byte-identical with SHA-256
  `3D3835701E61F6D3EC4D7D11E3A0DDEF0196D2265275F7A2D2710096A8A58F01`.
- `git diff --quiet -- frontend/pnpm-lock.yaml` returned zero.

### Docker

`docker build --target deps --file deploy/docker/frontend.Dockerfile .` passed.
The final exact build reported Node `v22.20.0`, pnpm `11.5.2`, verified 776 lock
entries, and installed 735 packages. Docker resolved the exact base tag to
`node:22.20.0-alpine@sha256:dbcedd8aeab47fbc0f4dd4bffa55b7c3c729a707875968d467aaaea42d6225af`.
The unnamed dependency-stage result was
`moby-dangling@sha256:6d81917b68f03ad5f57474effc2edde5263eca120d06ec566624339385f65a3e`.
Release-candidate base-image digest pinning remains owned by CH-1.7B/CH-5.5;
this packet pins the runtime version rather than claiming a release freeze.

### Frontend non-regression

- A post-change full Vitest run passed 70 files and 424 tests. One earlier run
  reproduced the already-known order-sensitive
  `ProjectCockpitPage > highlights documents used by open trade RFT` failure;
  an immediate full rerun passed. CH-0.6 owns deterministic remediation.
- Lint remained at the recorded baseline of 18 errors and 4 warnings.
- Build remained at the recorded three `TS6133` diagnostics. CH-0.5 owns them.
- Pinning did not add any new diagnostic or test-failure class.

### CI

GitHub Actions run `31752234413` reported:

- Node `v22.20.0` and pnpm `11.5.2` in `Report frontend toolchain`;
- successful setup, frozen install, and existing root typecheck command;
- successful Backend checks and Tender seed validation;
- the expected frontend lint failure at its unchanged baseline.

The run is intentionally not represented as a green frontend gate. CH-0.5 and
CH-0.6 must make typechecking real and clear the known lint/test baseline before
GATE-0.

## Rollback and residual risk

Rollback requires reverting both implementation commits together so package
metadata, CI, Docker, workspace policy, and the npm lockfile do not diverge.
No runtime data migration occurred. The candidate does not yet satisfy the
frontend quality gate; it establishes the deterministic toolchain on which that
gate will run.
