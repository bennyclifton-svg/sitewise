# CH-0.5 strict frontend typecheck evidence

## Identity

- Task: `CH-0.5`
- Started: `2026-08-13T23:06:32Z`
- Completed: `2026-08-13T23:19:44Z`
- Baseline source SHA: `bb53da8f`
- Implementation SHA: `cecd15b68cf9002e5bb65b79d284676b51ff9718`
- GitHub Actions run: `31753270371`

## Red baseline

From `frontend`:

- `pnpm.cmd exec tsc --noEmit --pretty false --listFiles` exited zero and
  listed zero files. The former root CI command was therefore a false-green
  check over a project-reference container.
- `pnpm.cmd exec tsc -p tsconfig.app.json --noEmit --strict --pretty false`
  exited two with exactly three `TS6133` diagnostics: one unused type import,
  one dead refresh helper, and one unused test import.
- the equivalent strict Node-config check exited zero with no diagnostics.
- TypeScript was `6.0.3`.

## Implemented contract

- `pnpm typecheck` now runs `tsc -b --pretty false`, traversing both referenced
  TypeScript projects.
- strict mode is explicit in both leaf configurations.
- CI runs the same command, preserves its exit status, and reports the
  TypeScript version, diagnostic count, and duration.
- frontend agent guidance names the same canonical command.
- the three genuinely unused symbols were removed without casts, suppression,
  ignored files, or compiler weakening.
- the root project-reference structure was retained.

No dependency or lockfile changed.

## Verification

### Local green proof

- `pnpm.cmd typecheck`: exit zero, zero diagnostics.
- A temporary `const deliberateTypecheckFailure: string = 1` made that exact
  command exit two with exactly one `TS2322`; the temporary line was then
  removed and the command returned green.
- `pnpm.cmd exec eslint src/pages/ProjectCockpitPage.tsx
  src/pages/ProjectCockpitPage.test.tsx`: exit zero.
- `pnpm.cmd exec vitest run src/pages/ProjectCockpitPage.test.tsx`: one file,
  seven tests passed.
- `pnpm.cmd build`: exit zero; 2,517 modules transformed and bundle-budget
  enforcement passed.
- `git diff --check`: exit zero.
- `frontend/pnpm-lock.yaml`: unchanged.

### Known full-suite baseline

An ordinary full `pnpm test` run reproduced the pre-existing
`ProjectCockpitPage > highlights documents used by an open trade RFT` race at
line 329: 69 of 70 files and 423 of 424 tests passed. The isolated page file
passes all seven tests, and CH-0.5 changed no behavior. CH-0.6 owns deterministic
remediation of this order-sensitive test before GATE-0.

### CI proof

GitHub Actions run `31753270371` reported in `Typecheck frontend`:

- TypeScript `6.0.3`;
- `TypeScript diagnostics: 0`;
- `Typecheck duration: 10s`;
- successful typecheck step.

Backend offline checks and Tender seed validation also passed. The frontend job
then stopped at its known lint baseline; CH-0.6 owns that work, so this run is
not represented as a green frontend or GATE-0 run.

## Rollback and residual risk

Rollback requires reverting the script, both leaf configs, CI, guidance, and
unused-symbol cleanup together. Doing so restores a false-green CI check and
reopens CH-0.5. The remaining lint and nondeterministic-test baseline is explicit
and must be cleared by CH-0.6.
