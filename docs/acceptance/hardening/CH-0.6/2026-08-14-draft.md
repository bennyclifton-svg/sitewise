# CH-0.6 draft evidence — deterministic frontend lint and tests

Status: **verified working-tree implementation; not complete and not committed**

- Task: `CH-0.6A` and `CH-0.6B`
- Operator: Codex `/root` with `/root/frontend_audit`
- UTC start: `2026-08-13T23:21:47Z`
- Last verification: `2026-08-13T23:52:38Z`
- Base/source SHA: `85efba60f5335ab8d7bbfdd592de2ff6a135aa8e`
- Result SHA: unavailable; Git staging/commit was rejected after the account reached its approval/usage limit
- Environment: Windows; Node `v22.20.0`; pnpm `11.5.2`; TypeScript `6.0.3`

## Red baseline

- `pnpm.cmd lint`: exit 1 with 15 errors and 4 warnings on the then-current
  post-CH-0.5 tree.
- Full Vitest: 69/70 files and 423/424 tests; the repository-highlight
  assertion in `ProjectCockpitPage.test.tsx` failed only under full-suite load.
- The page test alone passed 7/7, demonstrating an ordering/timing race.
- A new end-of-document parser regression test first failed because JavaScript
  does not implement `\Z` as an end anchor.

## Implemented working-tree scope

- Lint now fails on warnings with `eslint . --max-warnings 0`.
- React state resets use keyed child state where identity changes; inline editor
  refs are updated in ordered layout effects and opening editor values use lazy
  state.
- Workflow trace formatting moved out of a component module.
- The flaky repository assertion uses an eventual assertion, never a sleep.
- The procurement parser uses a valid JavaScript end-of-input assertion.
- Exactly two localized, justified `react-hooks/incompatible-library`
  suppressions remain immediately above TanStack Virtual calls; there is no
  file/config rule weakening.

The intended scope is 22 files. Its canonical manifest sorts each path and
records its `HEAD` Git blob ID (or `absent`) and current SHA-256, joins those
records with LF, and hashes the result with SHA-256.

- Scoped manifest SHA-256:
  `5b828a8ec58afb81ccbdaad8f4d0f9275eeadd0cbec98196a40b3efa0ea32f6a`

## Verification

From `frontend` on the final production source:

```powershell
pnpm.cmd lint
pnpm.cmd typecheck
pnpm.cmd build
```

All exited 0. TypeScript reported zero diagnostics. The production build
transformed 2,518 modules and passed enforced bundle budgets:

- initial cockpit: `228,890` gzip bytes, limit `256,000`;
- Tender workflow: `18,353` gzip bytes, limit `153,600`.

Three consecutive required `pnpm.cmd test` runs passed 72/72 files and 430/430
tests in `51.59s`, `52.57s`, and `52.48s`. A post-review fourth run also passed
430/430 in `52.86s`.

Task-scoped `git diff --check`: pass. `frontend/pnpm-lock.yaml` diff: none.

## Rollback and residual risk

No schema migration. Revert a behavioral component with its focused regression
test if necessary; do not weaken the warning-fatal lint command. A clean
exact-source run is still required after implementation commits A (`CH-0.6A`)
and B (`CH-0.6B`) are created.

This task remains `in-progress`. Required next steps are selective staging into
the two child commits, clean-checkout verification, SHA-specific evidence, and
the coordination commit.
