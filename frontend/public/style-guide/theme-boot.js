// Generated by scripts/sync-colour-tokens.mjs from the approved SiteWise colour specification.
/* Set the stored theme before first paint; storage key matches src/lib/theme.ts. */
(function () {
  var theme = 'dark';
  try {
    var stored = window.localStorage.getItem('clerk.colorTheme.v1');
    if (stored === 'light' || stored === 'dark') theme = stored;
  } catch { /* Blocked storage retains the default. */ }
  var root = document.documentElement;
  root.setAttribute('data-theme', theme);
  root.style.colorScheme = theme;
  var scheme = document.querySelector('meta[name="color-scheme"]');
  if (scheme) scheme.setAttribute('content', theme);
  var colour = document.querySelector('meta[name="theme-color"]');
  if (colour) colour.setAttribute('content', {"light":"#F9F7F3","dark":"#0A0A0A"}[theme]);
})();
